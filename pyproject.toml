[project]
name = "agentic-ai-crux"
version = "0.1.0"
description = "A notebook-first repository for learning agentic AI with the OpenAI SDK and OpenRouter."
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "ipykernel>=7.0.0",
    "jupyterlab>=4.4.0",
    "openai>=3.8.0",
    "python-dotenv>=1.1.0",
]

[tool.uv]
package = false
